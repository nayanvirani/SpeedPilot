# Shopify Partner app credentials (same values as backend/.env - the
# frontend only needs the API key, never the secret, since App Bridge
# session-token verification happens on the Laravel backend).
SHOPIFY_API_KEY=

# Laravel backend this frontend talks to for all app data.
BACKEND_URL=http://127.0.0.1:8000
